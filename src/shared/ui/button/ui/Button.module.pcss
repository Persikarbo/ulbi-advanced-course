.button {
  padding: 0;
  margin: 0;
  background: none;
  border: none;
  outline: none;
  cursor: pointer;
  max-height: 100%;

  &.bordered {
    border: 1px solid var(--themeInvertedPrimaryColor);
  }
}