@define-mixin textBig {
  font-size: 3rem;
  line-height: ratio(4rem, 3rem);
}

@define-mixin nlc {

  &:not(:last-child) {
    @mixin-content;
  }
}

@define-mixin resetLink {
  text-decoration: none;
  border: 0;
  color: inherit;
}